<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MeteoAPP.ListMeteoPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MeteoAPP.Models" 
    xmlns:viewModels="clr-namespace:MeteoAPP.ViewModels"
    x:DataType="viewModels:MeteoListViewModel">
    
    <ContentPage.Resources>
        <!-- Stile per ListView -->
        <Style TargetType="ListView">
            <Setter Property="SelectionMode" Value="None" /> <!-- Disabilita la selezione visiva -->
            <Setter Property="VisualStateManager.VisualStateGroups">
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal" />
                        <VisualState x:Name="Selected">
                            <VisualState.Setters>
                                <Setter Property="Background" Value="Transparent" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter>
        </Style>
    </ContentPage.Resources>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add" Clicked="OnAddCityClicked" />
    </ContentPage.ToolbarItems>
    
    <ListView x:Name="CitiesListView"
              ItemsSource="{Binding Cities}" 
              ItemTapped="OnCitySelected">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="local:City">
                <TextCell Text="{Binding Name}" Detail="{Binding Country}" />
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>